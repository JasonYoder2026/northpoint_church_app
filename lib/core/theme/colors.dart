import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const primary = Color(0xFF0D1B2A);
  static const secondary = Color.fromARGB(255, 102, 138, 126);
  static const accent = Color(0xFF006F64);

  // Neutrals
  static const lightBackground = Color.fromARGB(255, 219, 245, 237);
  static const lightSurface = Colors.white;
  static const lightTextPrimary = Color(0xFF0F172A);
  static const lightTextSecondary = Color(0xFF475569);

  // Neutrals
  static const darkBackground = Color.fromARGB(255, 15, 15, 15);
  static const darkSurface = Color.fromARGB(255, 65, 65, 65);
  static const darkTextPrimary = Color(0xFFE2E8F0);
  static const darkTextSecondary = Color(0xFF94A3B8);

  // Feedback
  static const success = Color(0xFF16A34A);
  static const warning = Color(0xFFF59E0B);
  static const error = Color(0xFFDC2626);
}
